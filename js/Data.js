function Data(component, p, frame){
  this.component = component;
  this.tm = new TransformManager(new Rectangle(-2, 2, 4, 4), component);
  this.p = p;
  this.frame = frame;
  //this.panel = new LayeredPanel() ? 
  
}
